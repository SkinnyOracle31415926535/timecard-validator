{
  "name": "timecard-validator-chatgpt-site",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "test": "node --check temporary-data-transfer.js && node --check transfer-theme.js && node --check timecard-store.js && node --check worker/index.js && node --test tests/temporary-data-transfer.test.cjs tests/transfer-theme.test.cjs tests/timecard-store.test.mjs tests/private-sync-retirement.test.cjs && npm run build"
  }
}
