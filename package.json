{
  "name": "timecard-validator-chatgpt-site",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "test": "node --check automatic-app-sync.js && node --check timecard-automatic-storage.js && node --check timecard-store.js && node --check worker/index.js && node --test tests/automatic-app-sync.test.cjs tests/timecard-store.test.mjs tests/private-sync-retirement.test.cjs && npm run build"
  }
}
