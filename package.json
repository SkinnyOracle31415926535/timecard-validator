{
  "name": "timecard-validator-chatgpt-site",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "test": "node --check timecard-store.js && node --check worker/index.js && node --test tests/timecard-store.test.mjs tests/private-sync-retirement.test.cjs && npm run build"
  }
}
